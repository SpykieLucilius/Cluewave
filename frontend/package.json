{
  "name": "cluewave-monorepo",
  "private": true,
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "heroku-prebuild": "npm ci --prefix frontend",
    "heroku-postbuild": "npm --prefix frontend run build && cp -r frontend/dist/* backend/src/main/resources/static/",
    "start": "java -Dserver.port=$PORT -jar backend/target/cluewave-backend.jar"
  }
}