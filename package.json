{
  "name": "cluewave-monorepo",
  "private": true,
  "engines": { "node": "20.x" },
  "scripts": {
    "heroku-postbuild": "npm --prefix frontend ci && npm --prefix frontend run build",
    "start": "node backend/src/index.js"
  }
}
